#planning of work day, scheduling appointment requirements

def scheduling_reqs(apts_booked)
  if apts_booked >= 4
  return "schedule today is fully booked"
  else:
  return "Able to schedule in an appointment"
  
